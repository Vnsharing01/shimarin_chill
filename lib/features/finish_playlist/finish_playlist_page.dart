import 'package:flutter/widgets.dart';

class FinishPlaylistPage extends StatelessWidget {
  const FinishPlaylistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}